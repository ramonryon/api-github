import styled from 'styled-components';

export const Container = styled.main`
    width: 100%;
    height: 100vh;
    background: linear-gradient(120deg, #130f40, #30336b);

    display: flex;
    align-items: center;
    justify-content: center;
    flex-direction: column;
`